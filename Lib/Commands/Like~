/*
* Run when Like requested
* Checks if OP
* If not also do dashes
* Adds like to notes
* Adds one to like counter
* - Adds like to OPs notes
* - Adds one to like counter
*
* DO NOT DO BOTH OF THE BELOW. THAT WILL CREATE A DUPLICATE ACTION
*
* IF OP
* Adds like to other Notes
* Adds one to like counter
* IF NOT OP
* - Adds like to OPs other notes
* - Adds one to like counter
*/
